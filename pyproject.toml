[project]
name = "rewrite-brain-segmentation-pytourch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pip>=25.0.1",
]
[tool.setuptools.packages.find]
where = ["src"]
